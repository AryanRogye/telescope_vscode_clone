{
  "name": "comfy-live-grep",
  "displayName": "live-grep",
  "description": "telescope live grep clone for strings",
  "version": "0.0.3",
  "publisher": "comfyduck667",
  "repository": {
    "type": "git",
    "url": "https://github.com/AryanRogye/telescope_vscode_clone"
  },
  "engines": {
    "vscode": "^1.91.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:comfy-live-grep.grep-files"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [{
      "command": "comfy-live-grep.grep-files",
      "title": "grep-files"
    }]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.91.0",
    "@types/mocha": "^10.0.7",
    "@types/node": "20.x",
    "eslint": "^8.57.0",
    "typescript": "^5.4.5",
    "@vscode/test-cli": "^0.0.9",
    "@vscode/test-electron": "^2.4.0"
  }
}
